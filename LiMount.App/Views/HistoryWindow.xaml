<Window x:Class="LiMount.App.Views.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Mount History - LiMount"
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Success/Failure status to color converter -->
        <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="FailureBrush" Color="#F44336"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Mount History"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#333"/>
            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="14"
                       Foreground="#666"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- History DataGrid -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#DDD"
                BorderThickness="1"
                CornerRadius="4">
            <DataGrid x:Name="HistoryDataGrid"
                      ItemsSource="{Binding HistoryEntries}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      AlternatingRowBackground="#F9F9F9"
                      RowBackground="White"
                      BorderThickness="0"
                      CanUserResizeRows="False"
                      CanUserAddRows="False">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Timestamp"
                                        Binding="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}"
                                        Width="150"/>

                    <DataGridTextColumn Header="Operation"
                                        Binding="{Binding OperationType}"
                                        Width="100"/>

                    <DataGridTemplateColumn Header="Status" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StatusDisplay}"
                                           FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Success}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Success}" Value="False">
                                                    <Setter Property="Foreground" Value="{StaticResource FailureBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Disk"
                                        Binding="{Binding DiskIndex}"
                                        Width="60"/>

                    <DataGridTextColumn Header="Partition"
                                        Binding="{Binding PartitionNumber}"
                                        Width="80"/>

                    <DataGridTextColumn Header="Drive"
                                        Binding="{Binding DriveLetterDisplay}"
                                        Width="60"/>

                    <DataGridTextColumn Header="Distro"
                                        Binding="{Binding DistroName}"
                                        Width="100"/>

                    <DataGridTextColumn Header="Details"
                                        Binding="{Binding DetailsDisplay}"
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Refresh"
                    Style="{StaticResource ModernButton}"
                    Command="{Binding RefreshCommand}"/>
            <Button Content="Clear History"
                    Style="{StaticResource ModernButton}"
                    Background="#F44336"
                    Command="{Binding ClearHistoryCommand}"/>
            <Button Content="Close"
                    Style="{StaticResource ModernButton}"
                    Background="#757575"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
